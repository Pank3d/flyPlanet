# Docker Guide - VLESS Reality Bot

Полное руководство по использованию Docker для развертывания Telegram бота.

## Как это работает?

### Архитектура

```
┌─────────────────────────────────────────┐
│          Docker Environment             │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │     vless-reality-bot             │  │
│  │   (Node.js 18 + Bot Code)         │  │
│  │                                   │  │
│  │  - Telegram Bot API               │  │
│  │  - QR Code Generator              │  │
│  │  - UUID Generator                 │  │
│  └───────────────────────────────────┘  │
│              ↓                          │
│         [PortMapping]                   │
│              ↓                          │
└──────────────┼──────────────────────────┘
               ↓
        Telegram Servers
               +
        Your VPS (Xray Server)
```

### Компоненты

1. **Dockerfile** (`bot/Dockerfile`)
   - Базовый образ: Node.js 18 на Alpine Linux
   - Устанавливает зависимости (telegram bot API, QRCode, UUID)
   - Копирует код бота
   - Запускает бота

2. **docker-compose.yml**
   - Описывает сервисы и их конфигурацию
   - Управляет переменными окружения
   - Настраивает сеть и логирование
   - Автоматический перезапуск при сбоях

3. **.env файл**
   - Хранит секретные данные (токены, ключи)
   - НЕ коммитится в Git
   - Используется docker-compose для передачи переменных

## Быстрый старт

### Шаг 1: Установите Docker

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo systemctl enable docker
sudo systemctl start docker

# Установите Docker Compose
sudo apt-get install docker-compose-plugin
```

**Windows/Mac:**
- Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop

### Шаг 2: Создайте .env файл

```bash
cd vless-reality-project
cp .env.example .env
nano .env
```

Заполните данные:
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
SERVER_IP=89.208.231.117
PUBLIC_KEY=ваш_публичный_ключ_xray
```

### Шаг 3: Запустите бота

```bash
# Собрать образ и запустить контейнер
docker-compose up -d

# Посмотреть логи
docker-compose logs -f bot

# Остановить
docker-compose down
```

## Подробное объяснение

### Что происходит при запуске?

1. **docker-compose up -d**
   ```
   ├─ Читает docker-compose.yml
   ├─ Загружает переменные из .env
   ├─ Строит Docker образ из bot/Dockerfile
   │  ├─ Скачивает Node.js 18 Alpine
   │  ├─ Копирует package.json
   │  ├─ Устанавливает npm зависимости
   │  └─ Копирует код бота (index.js)
   ├─ Создает контейнер vless-reality-bot
   ├─ Передает переменные окружения (BOT_TOKEN, SERVER_IP, PUBLIC_KEY)
   ├─ Запускает контейнер в фоновом режиме (-d)
   └─ Настраивает автоперезапуск (restart: unless-stopped)
   ```

2. **Внутри контейнера**
   ```
   ├─ Запускается Node.js
   ├─ Выполняется: node index.js
   ├─ Бот читает переменные окружения
   ├─ Подключается к Telegram API
   ├─ Ожидает команды от пользователей
   └─ Генерирует VLESS конфиги и QR-коды
   ```

### Файловая структура

```
vless-reality-project/
├── bot/
│   ├── index.js              # Код бота (ES6)
│   ├── package.json          # Зависимости Node.js
│   ├── Dockerfile            # Инструкции для сборки образа
│   └── .dockerignore         # Исключения при копировании
├── docker-compose.yml        # Конфигурация Docker Compose
├── .env                      # Секретные данные (НЕ в Git!)
├── .env.example              # Пример .env файла
└── DOCKER_GUIDE.md           # Это руководство
```

## Управление контейнером

### Основные команды

```bash
# Запустить контейнер
docker-compose up -d

# Остановить контейнер
docker-compose down

# Перезапустить контейнер
docker-compose restart

# Посмотреть статус
docker-compose ps

# Посмотреть логи (последние 100 строк)
docker-compose logs --tail=100 bot

# Следить за логами в реальном времени
docker-compose logs -f bot

# Зайти внутрь контейнера
docker-compose exec bot sh

# Пересобрать образ после изменений кода
docker-compose up -d --build
```

### Обновление бота

```bash
# 1. Остановите контейнер
docker-compose down

# 2. Обновите код (git pull или измените вручную)
git pull origin main

# 3. Пересоберите и запустите
docker-compose up -d --build

# 4. Проверьте логи
docker-compose logs -f bot
```

### Отладка

**Проверить работает ли бот:**
```bash
docker-compose ps
# Должно показать: State = Up
```

**Посмотреть логи ошибок:**
```bash
docker-compose logs bot | grep -i error
```

**Проверить переменные окружения:**
```bash
docker-compose exec bot printenv | grep -E 'BOT_TOKEN|SERVER_IP|PUBLIC_KEY'
```

**Проверить процессы внутри контейнера:**
```bash
docker-compose exec bot ps aux
```

## Преимущества Docker

### 1. Изоляция
- Бот работает в собственной среде
- Не конфликтует с другими приложениями
- Не засоряет систему зависимостями

### 2. Воспроизводимость
- Одинаково работает на любой ОС (Linux, Windows, Mac)
- Версии зависимостей зафиксированы
- Легко развернуть на другом сервере

### 3. Управление
- Один файл .env для всех настроек
- Простые команды для запуска/остановки
- Автоматический перезапуск при сбоях

### 4. Безопасность
- Секреты хранятся в .env (не в коде)
- .env не попадает в Git
- Контейнер изолирован от хост-системы

## Развертывание на VPS

### Вариант 1: Только бот в Docker

```bash
# На VPS
ssh root@YOUR_VPS_IP

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Клонируйте репозиторий
git clone https://github.com/yourusername/vless-reality-project
cd vless-reality-project

# Создайте .env
nano .env
# (вставьте ваши данные)

# Запустите
docker-compose up -d

# Проверьте
docker-compose logs -f bot
```

### Вариант 2: GitHub Actions + Docker на VPS

Обновите `.github/workflows/deploy.yml`:

```yaml
- name: Deploy bot with Docker
  uses: appleboy/ssh-action@v1.0.0
  with:
    host: ${{ env.VPS_HOST }}
    username: ${{ env.VPS_USER }}
    password: ${{ env.VPS_PASSWORD }}
    script: |
      cd ~/vless-reality-project

      # Обновить код
      git pull origin main

      # Создать .env
      cat > .env << EOF
      BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
      SERVER_IP=${{ secrets.VPS_HOST }}
      PUBLIC_KEY=${{ steps.extract.outputs.public_key }}
      EOF

      # Запустить через Docker
      docker-compose down
      docker-compose up -d --build
```

## Логирование

### Просмотр логов

```bash
# Последние 50 строк
docker-compose logs --tail=50 bot

# В реальном времени
docker-compose logs -f bot

# С временными метками
docker-compose logs -t bot

# За последние 10 минут
docker-compose logs --since 10m bot
```

### Логи хранятся:

- **Внутри контейнера:** stdout/stderr
- **На хосте:** `/var/lib/docker/containers/.../*.log`
- **Ограничение размера:** 10MB × 3 файла (настроено в docker-compose.yml)

## Мониторинг ресурсов

```bash
# Использование CPU, RAM, Network
docker stats vless-reality-bot

# Размер образа
docker images | grep vless

# Дисковое пространство
docker system df
```

## Безопасность

### .env файл - ВАЖНО!

```bash
# НИКОГДА не коммитьте .env в Git
echo ".env" >> .gitignore

# Права доступа только для владельца
chmod 600 .env
```

### Обновления

```bash
# Обновить базовый образ Node.js
docker-compose pull

# Пересобрать с новым образом
docker-compose up -d --build
```

## Troubleshooting

### Бот не запускается

```bash
# Проверьте логи
docker-compose logs bot

# Проверьте .env файл
cat .env

# Проверьте что контейнер создан
docker-compose ps
```

### Ошибка "network not found"

```bash
docker-compose down
docker network prune
docker-compose up -d
```

### Ошибка "port already in use"

Docker для бота не использует внешние порты, эта ошибка не должна возникать.

### Контейнер постоянно перезапускается

```bash
# Посмотрите причину
docker-compose logs --tail=100 bot

# Проверьте переменные окружения
docker-compose config
```

## Резервное копирование

```bash
# Сохранить образ
docker save vless-reality-project-bot:latest | gzip > bot-backup.tar.gz

# Восстановить образ
docker load < bot-backup.tar.gz
```

## Полезные ссылки

- Docker Documentation: https://docs.docker.com
- Docker Compose: https://docs.docker.com/compose
- Node.js Docker Best Practices: https://github.com/nodejs/docker-node

## Команды шпаргалка

```bash
# Старт
docker-compose up -d

# Стоп
docker-compose down

# Перезапуск
docker-compose restart

# Логи
docker-compose logs -f bot

# Пересборка
docker-compose up -d --build

# Статус
docker-compose ps

# Очистка
docker system prune -a
```

---

Теперь ваш бот работает в Docker! 🐳
